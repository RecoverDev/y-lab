spring:
  application:
    name: HabitManager
  datasource:
    url: jdbc:postgresql://localhost:5432/habitbase
    username: postgres
    password: password
    driver-class-name: org.postgresql.Driver
  jpa:
    generate-ddl: false
  liquibase:
    change-log: db/changelog/changelog.xml
    enabled: true